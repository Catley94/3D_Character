{
  "name": "ai-character-assistant",
  "version": "1.0.0",
  "description": "A cute AI companion that floats on your desktop",
  "main": "dist-electron/main.js",
  "scripts": {
    "build:helper": "cd foxy-input-helper && cargo build --release",
    "dev": "npm run build:helper && vite",
    "build": "npm run build:helper && tsc && vite build",
    "preview": "vite preview",
    "build:linux": "npm run build && electron-builder --linux",
    "build:mac": "npm run build && electron-builder --mac",
    "build:win": "npm run build && electron-builder --win",
    "build:all": "npm run build && electron-builder --linux --mac --win"
  },
  "keywords": [
    "ai",
    "assistant",
    "desktop",
    "overlay",
    "gemini"
  ],
  "author": "Sam",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "@types/node": "^25.0.10",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@tauri-apps/api": "^2.9.1"
  },
  "build": {
    "appId": "com.sam.ai-character-assistant",
    "productName": "AI Character Assistant",
    "directories": {
      "output": "release"
    },
    "extraResources": [
      {
        "from": "foxy-input-helper/target/release/foxy-input-helper",
        "to": "foxy-input-helper"
      }
    ],
    "files": [
      "dist/**/*",
      "dist-electron/**/*",
      "assets/**/*"
    ],
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Utility",
      "icon": "assets/icon.png",
      "maintainer": "sam@example.com",
      "executableArgs": [
        "--no-sandbox"
      ]
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.utilities",
      "icon": "assets/icon.png"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "assets/icon.png"
    }
  }
}
